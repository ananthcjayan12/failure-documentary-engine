# Shot division task

Project brief:
{{ brief.model_dump_json(indent=2) }}

Approved narration script:
{{ script.model_dump_json(indent=2) }}

Divide the narration into approximately 45–60 visual shots. Every narration segment must be covered.
A shot is a visual beat, not a master generated asset. Describe visual purpose, visual type, camera,
motion, overlays, and suspense function. Keep most shots 4–10 seconds; technical explanations may be
longer. Return JSON only matching ShotPlan.
