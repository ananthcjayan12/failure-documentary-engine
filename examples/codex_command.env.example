# The command must consume {prompt} and produce JSON at {output}.
# Adjust this to match the Codex CLI version installed on your system.
export FDE_LLM_COMMAND='codex exec --skip-git-repo-check --output-last-message {output} - < {prompt}'
